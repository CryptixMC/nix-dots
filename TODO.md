# TODO / Roadmap

Living roadmap for the Quickshell desktop (bar, launcher, greeter, theme system) and its remaining app-theming/ecosystem threads. Tree first for a 30-second scan, details below. `[x]` = shipped, `[~]` = designed but not built, `[ ]` = open thread.

## Tree

- **Shell & Theme System**
  - [x] Folder-based theme registry — `ultraviolet` + `catppuccin`, runtime-discovered ([§1](#1-theme-system))
  - [x] Wallpaper engine — static / gif / shader, per-theme, fullscreen-pause
  - [x] Live-sync: Hyprland borders, Ghostty colors, Zed chrome
  - [ ] Zen browser theming — real preset found, genuinely can't be added risk-free without a profile-migration decision (confirmed via a real eval assertion, not just a hunch — see §1)
  - [ ] Claude Desktop theming — hard limitation, documented not chased
  - [x] Runtime theme-switcher UI — the Launcher's Themes tab ([§3](#3-launcher-tabs))
- **Bar**
  - [x] Waybar + Walker fully retired, Quickshell is the only shell
  - [x] Swappable per-icon popup (`BarIcon.popupComponent`)
  - [x] Network switcher flyout — click Wi-Fi icon, see/connect to known + open networks, password prompt for new secured ones, `nmtui` escape hatch
  - [x] Bluetooth device flyout — click icon, connect/disconnect known devices, `blueman-manager` escape hatch for pairing new ones
- **Launcher** ([§3](#3-launcher-tabs))
  - [x] Tab bar — Applications / Games / Files / Themes
  - [x] Games tab — real Steam + Prism Launcher libraries
  - [x] Files tab — v1 tree/grid browser (flagged for a future redesign pass)
  - [x] Themes tab — cycle themes + per-theme wallpaper picker
  - [x] Icon/visual polish — `ThemedIcon` macOS-style tinting, themed glyph icons for files/folders, translucent card/icon backgrounds, real icon theme installed
  - [x] Tab-switch sizing bugs — whole-screen-height blowup, then 0-height collapse, then cross-tab height accumulation, all traced to `Loader` sizing semantics and fixed
- **Greeter** ([§4](#4-greeter))
  - [x] Quickshell greeter replacing ReGreet (password-only v1)
  - [x] Status icons — battery / brightness / volume / bluetooth
  - [x] Animated wallpaper
  - [x] Fingerprint prompt text shortened
  - [x] Brightness sysfs read confirmed safe under the `greeter` user — world-readable, no ACL issue ever existed
  - [x] Lock screen v1 built — `WlSessionLock` + real PAM (password/fingerprint), **deliberately not wired to any trigger yet** ([§5](#5-lock-screen))
- **Fingerprint / PAM** — architectural ceiling, prior art, security note; `pam_fingwit` confirmed not packaged in nixpkgs, not chased further ([§2](#2-fingerprint--pam))
- **AI Workstation** — Goose + eGPU-aware Ollama model routing ([§7](#7-ai-workstation))
  - [x] Phase 1a: eGPU hotplug hardening — kernel pin, undock-during-game guard, dead-config cleanup, headless ROCm compute verified live
  - [x] Phase 1b: Goose + Ollama model-routing plumbing — shared state file, dock/undock sync into `config.yaml`, gaming-time VRAM eviction, all live-tested end to end
  - [ ] `goose configure` + OpenRouter/Anthropic credentials — not run yet, blocks Goose actually routing anywhere but Ollama
  - [ ] Gaming path (`SUPER+G`) — state-write logic tested manually, full launch→play→exit cycle not yet run
  - [ ] Phase 1a remainder — D3 (docked) compute control, undocked clean-boot check, D3 surprise-unplug, suspend/resume
  - [x] Chat overlay v2 — `SUPER+D`/`SUPER+K`, persistent `goose acp` JSON-RPC backend (real streaming, tool-call visibility, send-while-waiting queue), **live-verified end to end** with two real bugs found and fixed via live testing ([§7](#7-ai-workstation))
  - [x] Chat session-history picker — `SUPER+H`, real ACP `session/list`/`session/load`, **live-verified**: list/select/preview/resume/continue all confirmed with real data ([§7](#7-ai-workstation))
  - [ ] Chat overlay Stages 3-6 (thinking/agent-mode/permission UI, model browser, subagent+Claude/Ollama routing, voice) — designed in `~/.claude/plans/i-want-super-d-optimized-crane.md`, not built yet
  - [x] Undocked model-routing config bug fixes — three real bugs found and fixed live (`sudo`/`PATH` × 2, plus `providers.<name>.model` not being synced), state reconciled for both undocked and (now) docked ([§7](#7-ai-workstation))
  - [x] Local coding-driver reliability, root-caused and fixed — `OLLAMA_CONTEXT_LENGTH` truncation bug + Goose's default-extension-set prompt bloat, both fixed; `opencode` evaluated and ruled out (confirmed upstream client bug against Ollama); `qwen2.5-coder:14b` via minimal-extension Goose is the working local coding setup ([§7](#7-ai-workstation))
  - [x] `goose-code` — real Goose recipe + wrapper tuned to act like Claude Code (verify-before-done, no fabricated tools, investigate don't guess), power-profile auto-boost, dock-aware model pick, live-verified on a real multi-file diagnostic task that closed an open §5 question, then live-verified again on real eGPU hardware at ~10x the CPU-only speed ([§7](#7-ai-workstation))
  - [~] Phase 2/3 remainder: model-browser panel, session picker, voice, conversation mode — designed, not built
- **Reference** — Omarchy, other Quickshell shells worth reading ([§6](#6-reference-repos))

---

## 1. Theme system

Everything pulls from `themes/<name>/{base16.yaml, theme.json?, wallpapers/, components/?}`. `base16.yaml` is mandatory and feeds both Stylix (build-time) and Quickshell (runtime, via `yq`). `theme.json` and `components/` are optional — a colors-only theme is valid (`ultraviolet` ships neither).

**Live-synced today** (no rebuild needed, flips the instant `ThemeState.setTheme()`/`cycleTheme()` runs):
- Hyprland active/inactive border colors (`hyprctl eval` + `hl.config(...)`, since this repo's Lua config backend doesn't support `hyprctl keyword`)
- Ghostty terminal colors, via a `config-file` include Ghostty always loads after its own `theme = stylix` baseline
- Zed **chrome only** (background/borders/tabs/panels/terminal ANSI) — written to `~/.config/zed/themes/quickshell-live.json` using Stylix's own generated `stylix.json` as the 141-key structural template. Syntax-highlighting colors and player-cursor colors deliberately stay static (whatever Stylix last generated for `ultraviolet`) — full syntax remapping is a lot of extra surface for a cosmetic win where the editor buffer itself is a small fraction of the screen most of the time.

**Zed's build-time half**: `zed.nix` sets `theme = lib.mkForce "Quickshell Live"`, overriding Stylix's own `theme = "Base16 <name>"` default. Needs `nh home switch` once to take effect (untested this pass whether an already-open Zed window hot-reloads the *content* of a custom theme file it already has selected — Zed's settings.json hot-reload is well-established, but a referenced theme file's content re-reading on change is a separate, unconfirmed mechanism).

**Zen browser — real option confirmed, genuinely can't be added risk-free**: `nix eval` against the live flake confirms `programs.zen-browser.profiles.<name>.presets.catppuccin.{enable,accent,flavor}` are real options (accent: 14 named colors, flavor: Frappe/Latte/Macchiato/Mocha) using the real `catppuccin/zen-browser` userChrome theme, not just GTK inheritance. Tried adding it as a second, `isDefault = false` profile alongside the existing self-managed live one (`~/.config/zen/huedeu9v.Default Profile`) — reverted after a real eval failure: the module hard-asserts "exactly one default Zen profile" the instant *any* profile is declared, with no concept of the pre-existing external profile to count against that assertion. So `isDefault = false` alone doesn't evaluate, and `isDefault = true` risks changing which profile actually launches by default. There's no zero-risk path here without first deciding how to bring the live profile under home-manager's management (name it explicitly, or accept the migration) — confirmed by evidence now, not just a hunch. Baseline GTK dark-mode (`gsettings ... prefer-dark`, already set in `hyprland.nix`'s autostart) covers native dialogs regardless.

**Claude Desktop**: Electron, no settings hook, no Stylix target. Only lever is GTK dialog chrome inheriting the system dark theme (already happening). Content-level theming isn't achievable from the Nix side — documented limitation, not a bug to keep chasing.

---

## 2. Fingerprint / PAM

- [x] Shortened fprintd timeout for `sudo`/TTY `login` (`modules/nixos/services/fprintd.nix`: `timeout = 5; max-tries = 2`). Explicitly disabled for `sshd` and `greetd`.
- **Architectural ceiling**: PAM's conversation model is sequential for a plain terminal — whichever module runs first blocks until success/timeout. No stock "race both, take whichever's ready."
- **Real prior art for a proper fix**: [Fingwit](https://github.com/xapp-project/fingwit)'s `pam_fingwit.so` — decides at runtime whether a scan is likely to succeed, skips straight to password if not, instead of blocking on a doomed read.
  - [ ] Evaluate swapping in `pam_fingwit.so` in place of the flat timeout. Checked: **not packaged in nixpkgs** under any name (`nix eval`'s own "did you mean 'finit'?" confirms no close match). Packaging an unfamiliar, security-sensitive PAM module blind (no way to test-drive it without risking login/sudo) isn't something to attempt autonomously — stays a real "someone needs to sit down and package + test this deliberately" item, not chased further.
- **Security note**: [CVE-2024-37408](https://linuxsecurity.com/news/security-projects/fingwit-biometric-authentication) — fingerprint-only auth on `su`/`sudo`/`polkit` can let a background process obtain privileges without a real scan prompt. Read before making fingerprint more automatic/prominent anywhere.
- The greeter's PAM conversation (via `Quickshell.Services.Greetd`) is genuinely sequential too — real concurrent fingerprint+password at the greeter isn't achievable without driving the PAM conversation directly (`Quickshell.Services.Pam`), which is architecturally a lock-screen-shaped project (see §5, now built) — not a greeter tweak.

---

## 3. Launcher tabs

`LauncherState.tabs` is a plain data list (`{id, label, glyph}`) — adding a fifth tab later is one entry, not a new code path. The tab row (pill-shaped, icon-only, hover/active-expands to icon+label) is built and live. The launcher box itself widens for Games/Files/Themes (960px vs. Applications' 564px) and each tab loads lazily (`Loader active: ...`) so Games/Files' background filesystem scans never run before that tab is opened.

### Games tab — built
- `GamesLibrary.qml` discovers real Steam (`appmanifest_*.acf`) and Prism Launcher (`instance.cfg`) libraries via a couple of batched `grep`/`find` calls each (not one process per game), cross-referencing cover art (Steam's `library_600x900.jpg`, Prism's per-instance `profileImage/` directory) separately by id.
- Recommended row (most-recently-played — the only honest signal without a real usage-scoring system), full library grid, and one grid per launcher, all confirmed rendering real games with real cover art.
- Search (shared with the other tabs) filters *within* each section rather than collapsing the layout.
- Adapter model is "one more Process block per launcher" in `GamesLibrary.qml`, not a plugin/script-file system — Lutris/Heroic aren't installed on this machine, so a heavier abstraction would be speculative. Adding one later is still a small, contained change.

### Files tab — built (v1; flagged for a future redesign pass)
- `FilesTab.qml`: `Qt.labs.folderlistmodel`'s `FolderListModel` backs both a left-side subfolder list + breadcrumb and a right-side grid of the current directory's full contents, defaulting to `$HOME`.
- v1 is a single navigable pane (descend/ascend), not a full expand/collapse multi-level tree — a real tree is more UI work than this pass needed, and this tab was explicitly called out for further design discussion before going further.
- Search filters the tree/grid in place via `nameFilters`, and separately surfaces matches *outside* the current directory as a flat path list via one bounded `find -iname` call (substring matching, not true fuzzy scoring).

### Themes tab — built
- Top row cycles installed themes (reuses `ThemeState`/`ThemeLoader` as-is, no new discovery).
- Second row shows the *active* theme's available wallpaper files (`ThemeEntryLoader`'s wallpaper-file discovery, `find`-based, excludes shader `.frag`/`.qsb` sources) and lets you pick one — `ThemeState.wallpaperOverrides` persists the choice per-theme, and `Theme.qml`'s `wallpaper` facade resolves it ahead of the theme.json-declared default (engine inferred from the picked file's extension). Confirmed live: picking a wallpaper takes effect immediately and survives switching to the other theme and back.
- No settings section: no `theme.json` currently declares any configurable options, so a generic toggle/dropdown-schema renderer would be untested speculative plumbing for zero real consumers — deferred until a theme actually wants to declare one, consistent with "colors-only theme has no settings."

### Icon/visual polish pass — built
- `ThemedIcon.qml` — `MultiEffect`-based macOS-style tinting (desaturate + colorize toward the active theme's accent) wraps every app icon in the Applications tab, replacing plain `IconImage`.
- Files tab folders/files render as themed Nerd Font glyphs (`` / ``) instead of relying on system icon-theme lookups per file type — sidesteps the "no icon theme installed" problem entirely for that tab and gives consistent, always-themed results.
- Translucent backgrounds (`ThemeDefaults.alpha(base02, 0.5)`) added behind Files-tab grid icons and Games-tab cover-art fallbacks, replacing solid fills.
- The old default/fallback icon for icon-less entries was replaced with a themed glyph rather than the generic broken-image look.

### Found and fixed along the way
- No icon theme package was actually installed system-wide (only cursor themes + empty `hicolor`) — every named-icon lookup across the *whole launcher*, not just the new tabs, was silently falling back to blank/generic icons despite `gsettings` already claiming "Adwaita". Added `adwaita-icon-theme` to `packages.nix` — needs `nh os switch` to take effect.
- `Image.source` needs a bare filesystem path, not a constructed `file://` URL, to handle names with spaces/brackets correctly (Prism instance "Arcadia [RPG] new" broke outright with the URL form even after percent-encoding).
- **Three-stage `Loader`/height bug**, all in `Launcher.qml`'s per-tab `Loader`s:
  1. Explicit `height: item.height` on a `Loader` fights Qt's own default behavior (a sized `Loader` force-resizes its loaded item to match) — created a feedback loop that froze `ThemesTab`'s height at 0 despite `implicitHeight` correctly computing 128.
  2. First fix attempt (`implicitHeight: root.height` inside `GamesTab`/`FilesTab`) was itself a genuine binding loop — `Item.height`'s own implicit default binding *is* `implicitHeight`, so anything that makes `implicitHeight` depend on `height`, even indirectly, silently freezes. Fixed by computing height once into an independent `readonly property real computedHeight` and binding both `height` and `implicitHeight` to that same property.
  3. After removing the `Loader`'s explicit height entirely, switching through tabs in sequence showed heights accumulating (Files 1286px, Apps 1654px) rather than resetting — an inactive `Loader`'s reported height wasn't reliably snapping back to 0. Fixed with `visible: active` on each `Loader`, since `Column` excludes invisible children from its layout sum regardless of their reported size. Verified live across a full Themes→Games→Files→Apps→Apps switch cycle with no accumulation.

---

## 4. Greeter

`quickshell-greeter/` — separate Quickshell tree, runs as the unprivileged `greeter` user pre-login via `services.greetd` + `cage` (no wlr-layer-shell there, so it's a single `FloatingWindow`, not `PanelWindow`). Deliberately decoupled from the daily-driver shell's theme registry (`quickshell-greeter/theme/Colors.qml` is its own hand-picked palette) — an in-progress edit to the desktop shell should never risk the login screen.

- [x] Password-only auth flow via `Quickshell.Services.Greetd`, session/user fixed (not enumerated at runtime).
- [x] Status icons — `modules/status/{Battery,Brightness,Volume,Bluetooth}.qml`, trimmed adaptations of the bar's own modules, read-only (no click actions — nothing meaningful to change pre-login besides Wi-Fi, which already has its own picker).
- [x] Animated wallpaper — `modules/greeter/Wallpaper.qml`, static+gif only (no shader machinery, not worth the build complexity for a login screen), reusing `catppuccin`'s existing `retro2_live.gif` rather than sourcing anything new.
- [x] Fingerprint prompt shortened — `AuthState.onAuthMessage` substitutes "Scan fingerprint" for any message containing "finger", instead of relying on fprintd's exact (long) wording.
  - Worth a follow-up check: `fprintd.nix` sets `security.pam.services.greetd.fprintAuth = false`, so a fingerprint prompt reaching the greeter at all was a little surprising. Didn't block the text fix, but the "why" is still open.
- [x] Brightness status icon's `/sys/class/backlight/.../brightness` read — checked directly: `/sys/class/backlight/intel_backlight/brightness` is `root:root`, mode `644` (world-readable). No seat ACL or `video`-group membership is needed for a plain read regardless of which user owns the session — closes this out, no fix was ever needed.

---

## 5. Lock screen

Architecturally nothing like the greeter — greetd/cage only run **pre-login**. A lock screen has to run **inside the already-authenticated session**, using `Quickshell.Wayland`'s `WlSessionLock` / `WlSessionLockSurface` (`ext-session-lock-v1` — confirmed present in the installed Quickshell 0.3.1 qmltypes).

### v1 built (2026-09-12) — deliberately not wired to any trigger yet

- `quickshell/modules/lock/{LockService.qml, LockView.qml, qmldir}` + `modules/nixos/services/quickshell-lock.nix` (new PAM service).
- `LockService.qml` drives `Quickshell.Services.Pam`'s `PamContext` directly (not `Quickshell.Services.Greetd` — that's greetd's own separate pre-login protocol) against a new `security.pam.services.quickshell-lock` service. Phase machine (idle/prompting/authenticating/failed) mirrors `quickshell-greeter/modules/auth/AuthState.qml`'s proven-live shape, adapted to `PamContext`'s API (confirmed via the installed qmltypes, not guessed): `config`, `user`, `message`, `responseRequired`/`responseVisible`, signals `completed(result)`/`error`/`pamMessage`, methods `start()`/`abort()`/`respond()`.
- The PAM service declaration is a bare `security.pam.services.quickshell-lock = {};` — confirmed via a real build that this alone produces a correct, complete stack (`pam_fprintd.so` sufficient, falling through to `pam_unix.so` for password, `pam_deny.so` otherwise) — `fprintd.nix`'s existing blanket `fprintAuth` default applies automatically, so fingerprint-or-password comes for free without saying so explicitly.
- `LockView.qml` (per-screen lock surface content, parented onto `WlSessionLockSurface.contentItem`): clock, prompt/error text, password field. v1 background is a flat themed color, not the full `Wallpaper.qml` engine — that component is tightly coupled to being its own `PanelWindow`, not something to re-parent into a lock surface's content item for this pass. No Escape-to-dismiss anywhere, unlike every other overlay in this repo — a lock screen must not be dismissible without real authentication.
- **Deliberately shipped inert**: nothing calls `LockService.lock()` from a keybind, idle timeout, or `loginctl lock-session` handler. The only trigger is a manual IPC call (`quickshell ipc call lock lock`) registered in `shell.qml`, meant to be run by hand once someone's ready to test the unlock path. A broken unlock path here has real consequences — getting stuck at a lock screen with no way back in — so this stays complete-but-untested-live rather than auto-wired, exactly the "build a switchable copy, don't make it live" treatment this kind of change needs.
- **Likely resolved via a local-model investigation, not yet a live test**: whether `pam.start()` can just be called again directly after a `Failed` completion, or needs `active` toggled first. §7's local coding-agent had `qwen2.5-coder:7b` read the real `quickshell-service-pam.qmltypes` and this file directly (no hints given) — neither `start`'s nor `active`'s type signature documents a reset requirement, and it concluded (high confidence, but from type signatures alone, not a live PAM conversation) that calling `pam.start()` directly is fine. Treat this as strong supporting evidence, not a replacement for actually testing the unlock path live — still confirm on the very first real test.
- Not carried over from Omarchy's reference (`shell/plugins/lock/Service.qml`/`LockView.qml`, ~840 lines total) since that source wasn't locally fetchable this session, so this is an original implementation grounded in the real Quickshell APIs rather than a port: stranded-lock recovery, a stabilize-timer before engaging, DPMS/idle reconciliation per monitor, and animated/video backgrounds are all still open — real ideas worth revisiting once the core password/fingerprint flow has been tested live at least once.

---

## 6. Reference repos

- **[Omarchy](https://github.com/omacom/omarchy)** (branch `quattro`) — real Quickshell-based shell with a genuine plugin architecture (`shell/plugins/*/manifest.json` + isolated QML). Coupling to Omarchy-specific behavior mostly shows up as external CLI calls rather than embedded logic, so most of it is reference/re-derive material rather than drop-in — the lock screen (§5) is the one piece confirmed directly adoptable near-verbatim. No tab-based launcher there (its menu is a hierarchical drill-down, not Spotlight-style tabs) — the launcher tab design (§3) is original.
- **[doannc2212/quickshell-config](https://github.com/doannc2212/quickshell-config)** — Ã  la carte reference bundling a status bar, launcher, notification daemon, and a runtime theme switcher with 206 bundled themes. Concrete prior art if the Themes tab (§3) grows a "browse community themes" feature later.
- **[SirAllap/quickshell-popups](https://github.com/SirAllap/quickshell-popups)** — theme-aware popup widgets including an existing `custom/claude-usage` module — prior art if a Claude-usage bar widget ever gets built.
- [Hyprland wiki: App Launchers](https://wiki.hypr.land/Useful-Utilities/App-Launchers/) / [Status Bars](https://wiki.hypr.land/Useful-Utilities/Status-Bars/) — general ecosystem, worth periodic re-checking.

---

## 7. AI Workstation

Goal: one system-level assistant (Goose, local Ollama + cloud fallback) that's aware of this laptop's actual hardware state — an AMD RX 6800 XT eGPU that's only sometimes attached over Thunderbolt — instead of a pile of disconnected chat/coding tools. Staged: 1a hardens the eGPU itself, 1b wires up Goose + routing headlessly, 2/3 (not started) build a Quickshell UI on top. Full plan/findings log: `~/.claude/plans/lets-work-on-this-quizzical-crane.md`.

### Phase 1a — eGPU hotplug hardening — built
- `modules/nixos/hardware/amd.nix` already had a working hotplug system (udev rules on PCI vendor/device ID, `egpu-bar-fix.service`, `egpu-eject.service`, AER/ASPM kernel-param workarounds) — this pass closed specific gaps rather than building from scratch.
- `boot.kernelPackages` pinned to `pkgs.linuxPackages_6_18` (verified via `nix eval` to be a no-op today, 6.18.50 either way) — stops a future `nix flake update` from silently shifting the kernel out from under the Thunderbolt/amdgpu workarounds.
- `egpuEjectScript` gained a Stage 1.5: refuses to eject (clear `hyprctl notify` instead) if `gamescope` is running, since a live game's own DRM context could hang the unbind same as ROCm's.
- Deleted `modules/nixos/apps/ollama.nix` — dead, unimported, hardcoded `acceleration = "cuda"` (wrong for this AMD-only host). Live config is `modules/nixos/services/ollama.nix`.
- **Live-verified**: ROCm compute genuinely lands on the eGPU with zero displays attached to it (`ollama ps` → `100% GPU`) — closes the one gap the codebase's own comments flagged as never actually checked. Surprise-unplugging the cable (no `SUPER+SHIFT+U`) in that same headless state did **not** hang Hyprland — the primary safety bar for that state held.

### Found and fixed along the way (Phase 1a)
- **The udev backstop doesn't reliably re-fire after a crash.** After the surprise-unplug above, `egpu-eject.service` never fired via its remove-triggered udev rule at all (kernel handled teardown solo, with real but non-fatal errors: `ring kiq test failed`, a `GPU reset` that failed, a kernel `WARNING` in `kfd_device_queue_manager.c`). Reattaching later in the same boot, `egpu-bar-fix.service`'s add-triggered rule *also* didn't fire (`udevadm info` showed `USEC_INITIALIZED` still pointing at the original attach, not the reattach) — the kernel rebinds the driver natively either way, so it looks like everything's fine, but none of the higher-level automation runs.
- **Root cause: a crashed removal leaves ROCm/KFD compute dead, not just the udev events.** `rocminfo` post-crash showed *zero* GPU agents despite `amdgpu` being cleanly bound with a valid BAR — display/PCI-level recovery is cosmetic, compute is gone until reboot. This is also why a model actively loaded at the moment of removal leaves an **unkillable orphaned `llama-server` process** (survives both `systemctl restart` and a full `systemctl stop` of `ollama.service`, despite correct `KillMode=control-group`) — it's genuinely stuck on a kernel-level wait for hardware that vanished.
- Practical upshot: "reboot after any surprise/crashed eGPU removal" is a hard requirement for this whole automation stack to be trustworthy, not a nice-to-have. Not yet turned into an automated health check (e.g. comparing `rocminfo` agent count against `lspci` GPU presence) — worth doing before leaning on this further.

### Phase 1b — Goose + Ollama model routing — built
- `modules/nixos/apps/ai-workstation.nix` (new): `/run/ai-workstation/state.json` (tmpfs, re-derived every boot — no impermanence exists in this repo so nothing else needed persisting), `ai-workstation-{dock,undock}-sync.service` oneshots with hardcoded `llmfit`-derived model tags (`qwen2.5-coder:7b` undocked, `qwen3.6:latest` docked — see file comments for why the docked pick overrides `llmfit`'s own sparse database), scoped NOPASSWD sudo rules mirroring the existing `egpu-eject` pattern.
- `modules/home-manager/apps/goose.nix` (new): `goose-cli`, `llmfit`, and a wrapped `goose-desktop` (`symlinkJoin`+`makeWrapper`, same idiom as `zed.nix`) that runs state-sync before every launch. Ships `goose-state-sync` (rewrites `~/.config/goose/config.yaml`'s `GOOSE_PROVIDER`/`GOOSE_MODEL` in place) and `ai-workstation-gaming-{start,stop}`.
- Hooked into `amd.nix`'s existing `egpu-bar-fix`/`egpu-eject` scripts (fire-and-forget, `--no-block`) and the `SUPER+G` keybind (`ai-workstation-gaming-start && gamescope ... ; ai-workstation-gaming-stop`), reusing the same eGPU detection rather than building a second one.
- **Live-verified end to end**: state file write → `config.yaml` rewrite → desktop notification, for both dock and undock paths, on real hardware.
- Claude access deliberately kept **out** of the automated routing (Ollama ⇄ OpenRouter only) — `goose session --help` confirms a first-class `claude-code` provider exists (validates the ACP/subscription-reuse idea), to be wired in manually once tried against the real Pro/Max subscription.

### Found and fixed along the way (Phase 1b)
None of these showed up in `nix flake check` or `nixos-rebuild build` — only live-testing caught them:
- `egpuBarFixScript`'s `PATH` was missing `pkgs.systemd` (worked before by ambient-PATH luck).
- The new sync scripts' `PATH` was missing `pkgs.bash`, so `env XDG_RUNTIME_DIR=... bash -lc ...` failed outright (`env: 'bash': No such file or directory`).
- **The real one**: `yq '.field' state.json` without `-r` returns the value with literal embedded quote characters (`'"docked"'`, not `docked`) — `mikefarah/yq` preserves the source's quoted scalar "style" when reading JSON. This silently broke every state comparison and fed malformed expressions into the later `yq -i` config writes (`lexer: invalid input text "ollama\"\""`). Fixed by adding `-r` everywhere the state file is read.
- `hyprctl notify` inside `goose-state-sync` silently no-op'd (`HYPRLAND_INSTANCE_SIGNATURE not set`) since a login shell invoked via `runuser` doesn't inherit the desktop session's env — fixed with the same signature-discovery trick `amd.nix`'s `hyprctl_user()` already uses.
- `goose configure`'s Ollama-provider model picker validates the entered tag against its own known-models list, not a live query of the running Ollama instance — entering `qwen3.6:latest` (a real, installed, working model, confirmed via `ollama list` and the raw `/api/tags` response) failed with `Model not found`, and the wizard fell back to defaulting the provider selection to Goose's bundled "Local Inference" (its own llama.cpp runtime) on retry. Explicitly re-selected **Ollama** via "Search all providers..." — confirmed the right call, since Goose's bundled runtime would bypass every bit of eGPU-aware routing built this session (it has its own separate model cache/process, no `ollama stop`-based VRAM eviction, none of tonight's hotplug integration applies to it).

`goose configure` resolved: Ollama selected, host `localhost` accepted, `qwen3.6:latest` set via direct `config.yaml` edit (not through the wizard's known-models picker, which never accepts it — see above).

### Goose capability upgrade — built (2026-09-12)

Goal: make Goose capable enough to actually take over the remaining work below — subagent delegation, real browser automation, desktop-state access — using the already-proven `qwen3.6:latest` (Ollama, docked) as the driving model.

- Reclaimed ~18GB: `goose configure`'s earlier known-models-validation detour (see above) had silently downloaded a full model, not just a config stub — `unsloth/gemma-4-26B-A4B-it-GGUF:Q4_K_M`, 17GB in `~/.cache/huggingface/hub/` plus a 1.2GB duplicate under `~/.local/share/goose/models/`. `goose local-models delete` only unregisters a model from Goose's own registry — it does **not** remove the downloaded weight files — had to `rm -rf` both paths manually to actually free the space.
- `~/.config/goose/config.yaml` (live, not Nix-managed — direct `yq -i` edits): enabled `orchestrator` (pairs with the already-enabled `summon` for real subagent session management), disabled the stray `local` provider left over from the wizard detour.
- Registered a real `playwright` extension (`pkgs.playwright-mcp` 0.0.76, already present in this flake's pinned nixpkgs) as a `type: stdio` entry in `config.yaml` — `--browser firefox --headless --image-responses omit` (the last flag forces accessibility-snapshot responses instead of screenshots, for token efficiency, matching this project's original Phase 3 intent). No existing Goose config on this machine used `type: stdio` before, so there was no local schema to copy — the exact required field name (`cmd`) was confirmed by grepping the compiled `goose` binary's own error strings (`strings ~/.nix-profile/bin/.goose-wrapped | grep cmd` → `stdio extension is missing required "cmd" field`) rather than guessed blind, then validated live: `goose run` starts cleanly with the new block (no parse error) and a live test round-tripped a real `Playwright.browserNavigate` call.
- `computercontroller` (already enabled) turned out to be **document processing (DOCX/PDF/XLSX), not desktop/mouse/keyboard control** — a wrong assumption caught by actually listing its tools in a live session rather than trusting its one-line description. Real "desktop control" for this setup is just `Developer.shell` (already enabled, full command execution) plus whatever CLI tools are already on the system (`hyprctl`, `grim`, etc.) — no extra extension needed.
- `Summon.delegate()` (subagent dispatch) and `Orchestrator` (session management) syntax confirmed via a live `list_functions` call, not assumed from the one-line extension descriptions.

### Found and fixed along the way (Goose capability upgrade)
- Non-interactively testing Goose (`goose run -t "..."`) against `qwen3.6:latest` is **slow enough to make scripted verification impractical** — a simple browser-navigation round trip took several minutes end to end (36B MoE model, 35%/65% CPU/GPU split per `ollama ps`, plus `code_execution` mode's own overhead of writing/running code against the tool namespaces rather than plain function-calling). Background-task completion notifications also fired before the underlying process had actually exited — repeatedly, not just once. Don't trust "completed" as proof output has stopped changing; check the process list (`ps -eo pid,etime,cmd | grep "goose run"`) every time.
- `goose run -t "..."` (without `-s`/`--interactive`) is genuinely single-shot: if the model responds with a clarifying question instead of acting, the process just exits — there's no one to answer it. The first full handoff attempt hit exactly this: the model read both context docs, correctly restated every rule, then asked to have the file contents pasted again instead of just re-reading them itself, and the session ended right there. Fix: the handoff prompt must explicitly forbid this ("you already have file read access, never ask me to paste file contents, just read them and proceed").
- **Goose's `claude-code` and `claude-acp` providers hand the entire tool loop to Claude Code's own native tools (`Read`/`Write`/`Bash`/`Agent`/etc.) — Goose's own extensions (`Summon`, `Orchestrator`, `Playwright`, `Developer`) are only available when a plain LLM provider (`ollama`, `openai`, raw `anthropic`) drives Goose's own tool-calling loop.** Confirmed by two separate live tests: both providers correctly refused to fabricate a `Summon.delegate` call it didn't actually have, and used their own native tool instead. This means Claude-as-driver and Goose-native-subagent-delegation are mutually exclusive within one session — there's no way to get both at once.
- Goose has genuine native ACP provider support compiled into the binary (found via `strings` on the binary, not documented anywhere locally): `claude_acp`, `amp_acp`, `codex_acp`, `copilot_acp`, `pi_acp`. `--provider claude-acp` with `CLAUDE_CODE_EXECUTABLE=$(which claude)` set genuinely authenticates via the local Pro/Max subscription (fast, confirmed "I'm Claude Sonnet 5") — distinct from the `claude-code` provider, which shells out to the `claude` CLI in a different (non-session) mode.
- The `claude-code` provider's first test hit `"You're out of usage credits"` with model `claude-fable-5-1[1m]` (the wizard-configured default) — but the raw `claude` CLI worked fine at the same moment, and `claude-code` with a plain model id (`claude-sonnet-4-6`, no `[1m]` suffix) also worked immediately. The `[1m]` (1M-context) variant is very likely metered separately from normal subscription usage — not a real account-wide limit.
- `GOOSE_SUBAGENT_PROVIDER`/`GOOSE_SUBAGENT_MODEL` (found the same way, via `strings`) let a session's `Summon.delegate()` calls run on a **different** provider/model than the primary session — confirmed live in both directions: an Ollama-primary session genuinely delegating to a `claude-code` subagent, and (attempted) the reverse. This is the real mechanism for "cheap primary, expensive subagent only when needed" or vice versa — not something documented anywhere, found by reading the compiled binary's own strings.
- **Local-model tool-calling reliability varies enormously and isn't correlated with model size the way you'd expect** — tested four combinations as primary driver for `Summon.delegate`:
  - `qwen3.6:latest` (36B MoE): correct execution, genuinely completes delegated tasks — but painfully slow (multi-minute per turn).
  - `llama3.2:3b`: fast, but hallucinates fake pseudocode instead of calling real tools at all.
  - `qwen2.5-coder:14b` (dense, tool_use-tagged, llmfit-recommended): fast (6-14s), but prints the tool-call JSON as literal text instead of actually invoking it — happens identically with `code_execution` mode on or off, so it's this model's Ollama packaging, not a Goose config issue.
  - `qwen3-coder` (30B MoE, llmfit's top-ranked tool_use pick): fast (~30s), genuinely executes real tools (confirmed via a real dispatch error, not hallucinated text) — but when it made a recoverable calling-convention mistake, it correctly diagnosed the error, looked up the right interface, and then just stopped and asked what to do instead of retrying with what it had just learned.
  - `devstral` (Mistral's dedicated agentic-coding model — the most purpose-built candidate on paper): doesn't attempt tool calls at all with `code_execution` on (pure chat, suggests `wc -l` in prose instead), and **hangs completely** (zero output, full timeout) with `code_execution` off. Same complete hang reproduced for `qwen3-coder` with `code_execution` off. So disabling `code_execution` isn't a safe fallback for models that fumble it — it broke two more models worse than it fixed.
  - **`qwen3.6:latest` is not reliably correct either — it's inconsistent, not just slow.** A repeat of the exact same delegation test that passed earlier failed on a later run, with the model wandering into a completely unrelated tangent ("I'll help you create a Python code review guide") instead of completing the task. Retracts the earlier "kept qwen3.6 since it's the only one that actually finishes the task" conclusion — none of the five local models tested tonight are a fully reliable primary driver for genuine multi-step agentic delegation. `qwen3.6:latest` remains the default anyway (best of a flawed set, and explicitly requested), with `GOOSE_SUBAGENT_PROVIDER=claude-code`/`GOOSE_SUBAGENT_MODEL=claude-sonnet-4-6` as the fallback path for harder work — this is a deliberate compromise, not a solved problem.
  - **Found `GOOSE_TOOLSHIM`/`GOOSE_TOOLSHIM_OLLAMA_MODEL`** (via `strings` on the compiled binary, undocumented locally): a real `goose::providers::toolshim::OllamaInterpreter` mechanism meant to take a primary model's freeform "detectable JSON-formatted tool request" text and convert it into a real, executable tool call via a smaller secondary interpreter model. Configured it (`GOOSE_TOOLSHIM: true`, `GOOSE_TOOLSHIM_OLLAMA_MODEL: qwen2.5-coder:7b`) and retested `devstral` with it — no change, still hangs. The toolshim didn't fix any of tonight's failures; left the config keys in place (harmless when unused) but didn't chase this further.
  - **Claude-code/claude-acp DO expose Goose's real MCP-server-backed extensions (like `playwright`) — they only lack Goose's own *internal* extensions** (`Summon`, `Orchestrator`, `Developer`, `Computercontroller`). Corrects the earlier blanket "Claude-as-driver and Goose's extension system are mutually exclusive" claim — it's specifically true for Goose-internal extensions (no MCP server backing them, nothing for an external agent to attach to) but not for genuine `type: stdio` MCP extensions, which Claude Code can use directly since it natively supports MCP. Confirmed live: a `claude-code`/`claude-sonnet-4-6` session correctly used `Playwright.browserNavigate`/`browserSnapshot` end-to-end (loaded the tool schemas, navigated to example.com, read back the real title/heading) — **this closes the "full playwright round-trip" verification item below.**
- Disk got tight from all the model pulls during this comparison (95%+, ~38GB free at one point) but was freed back up (155GB) before the `devstral`/`qwen3-coder` pulls — keep an eye on `df -h /home` before pulling more models regardless.
- **Live-reproduced the udev/ROCm crash-persistence bug from earlier tonight, for real, from these test runs**: one of the `ollama`-backed `goose run` invocations against the eGPU left a `llama-server` child (confirmed via `/proc/<pid>/status`, parented to the legitimate `ollama.service` — PID 4045, running since the last reboot) stuck in genuine `D` (uninterruptible sleep) state after the eGPU was disconnected mid-request. Unkillable, and `ollama ps`/`systemctl status ollama.service` both hang or time out trying to enumerate the process tree because of it. **Not fixed** — needs a reboot, which wasn't done autonomously (too disruptive without the user present to handle any fallout). `ollama.service` itself keeps serving unrelated requests fine in the meantime (confirmed `ollama list` and fresh completions both still work) — it's specifically anything that needs to enumerate/stop the wedged runner that hangs.

### Phase 3 chat overlay v1 — built, now live-verified (2026-09-12)

Started once GPU/Ollama work was explicitly paused mid-session (the model-comparison instability above) — pure QML/Nix, no GPU or Ollama-daemon involvement, so safe to build regardless of eGPU/ollama state.

- New module `quickshell/modules/chat/{ChatState.qml, ChatOverlay.qml, qmldir}`, following `Launcher.qml`'s established structure exactly: singleton state (`visible`, `busy`, `messages`, a per-Quickshell-instance `sessionName`) + a centered `PanelWindow` on `WlrLayer.Overlay`, shown/hidden via the same `IpcHandler`-toggle convention as the launcher/theme binds (`SUPER+K` → `quickshell ipc call chat toggle`).
- Talks to the real `goose` CLI: one `goose run --name <session> [--resume] -t "<message>"` invocation per sent message (not a single long-lived interactive process — Goose's actual stdin-driven interactive protocol was never confirmed this session, and per-message spawn is the shape already proven working in tonight's testing). `--resume` is correctly skipped on the very first message of a session (nothing exists yet to resume).
- `Process`/`SplitParser` output is buffered and filtered (drops the CLI's own banner-art lines) and appended as one message bubble per turn, not one per line — avoids fragmenting a single response across dozens of tiny bubbles.
- **Live-verified end to end (2026-09-12, undocked — no eGPU attached)**: toggled via the real `SUPER+K`-equivalent IPC call in a real running Hyprland+Quickshell session, typed a real message into the focused `TextInput` (via `nix run nixpkgs#wtype`, since no input-simulation tool was already installed), watched `ChatState.busy` flip the input border purple and show "goose is thinking…", and got a real rendered response bubble back from `qwen2.5-coder:7b` (the undocked model) — "you: what is 7 times 6? answer in one short sentence." → "goose: 7 times 6 equals 42." Screenshotted at each stage (open/empty, sent/busy, responded). `chatInput.forceActiveFocus()` genuinely does grab compositor keyboard focus for the layer-shell surface — confirmed by ruling out a false negative first (see below).
- **Found and fixed along the way**: the very first typing attempt silently went nowhere (no text appeared in the box) — not a focus bug in `ChatOverlay.qml` itself, but a stray `slurp -or` process left running as an exclusive-keyboard-interactivity overlay layer from an earlier interrupted `hyprshot` screenshot call. `hyprctl layers` showed it sitting above the chat overlay's own layer, silently swallowing all keyboard input. Killing it fixed typing immediately. Worth remembering for any future live Quickshell UI testing in this repo: check `hyprctl layers` for stray layers before concluding a focus mechanism is broken.

### Undocked model-routing state — fixed and reconciled live (2026-09-12)

Done with no eGPU attached, at the user's request — a real live test of the undocked path specifically, not something the docked path needed.

- **Found and fixed: `ai-workstation-gaming-stop`'s `sudo` calls were silently broken.** Its `PATH` override (`modules/home-manager/apps/goose.nix`) listed `pkgs.sudo` — the raw, non-setuid Nix store binary — ahead of the inherited `PATH`, so plain `sudo` inside that script always resolved to a binary that can't actually elevate (`sudo: ... must be owned by uid 0 and have the setuid bit set`), instead of `/run/wrappers/bin/sudo` (NixOS's real setuid wrapper, already on every session's inherited `PATH`). Fixed by dropping `pkgs.sudo` from the explicit `PATH` entirely.
- **Found and fixed: even with the right `sudo` binary, the NOPASSWD rule wasn't matching.** `security.sudo.extraRules` (`modules/nixos/apps/ai-workstation.nix`) grants NOPASSWD for the *exact absolute path* `${pkgs.systemd}/bin/systemctl start ...`, but `ai-workstation-gaming-stop` and the `SUPER+SHIFT+U` eGPU-eject keybind (`modules/home-manager/wm/hyprland.nix`) both called bare `sudo systemctl ...`. Even though `systemctl` resolves via `PATH` to that exact same binary, sudo's NOPASSWD command matching doesn't follow `PATH` resolution — it compares the literal string. Confirmed by direct A/B test: `sudo -n systemctl start ...` prompted for a password every time, `sudo -n /nix/store/.../systemctl start ...` succeeded instantly. Fixed both call sites to use `${pkgs.systemd}/bin/systemctl` explicitly.
- With both fixed, `ai-workstation-gaming-stop` was re-run live and correctly reconciled state with zero prompts: wrote `/run/ai-workstation/state.json` (`state: undocked`, model `qwen2.5-coder:7b`, real `lspci`-checked hardware presence), and `goose-state-sync` correctly rewrote `~/.config/goose/config.yaml`'s `GOOSE_PROVIDER`/`GOOSE_MODEL` to match — closing a real gap where, before this session, the config had been stuck on the *docked* model (`qwen3.6:latest`, 23GB) despite no eGPU being attached, because nothing had reconciled state since the last dock/undock hardware event (there's still no boot-time reconciliation — only event-triggered, a known gap, not addressed this pass).
- **Also found: `qwen2.5-coder:7b`, the model `ai-workstation.nix` designates for the undocked tier, had never actually been pulled** — `ollama list` only had the `14b` tag. Pulled it live (4.7GB). It hadn't been part of the four-model tool-calling comparison logged above either (only `14b` was tested, and failed); this session's chat-overlay test is its first real exercise, and it worked correctly for plain conversational use (no tool-calling was exercised by the test question).
- Both fixes verified via `nh home switch` (rebuild succeeds, `sudo` dropped from that package's closure) and a full `nix flake check`.

### Chat overlay v2 — ACP backend migration (2026-09-12, Stage 0+1 of the chat-expansion plan)

Full plan: `~/.claude/plans/i-want-super-d-optimized-crane.md` (7 stages: keybinds, ACP backend, session history, chat UX, subagent/Claude-Ollama routing, model browser, voice — this entry covers the first two, done and live-verified; the rest are still open).

- **Keybinds**: `SUPER+D` added as the primary chat toggle (`SUPER+K` kept as an alias), `SUPER+H` added for a session-history picker (target doesn't exist yet — confirmed it fails gracefully, `"Target not found"`, exit 0).
- **Backend rewritten from one-`goose run`-process-per-message to a persistent `goose acp` JSON-RPC session** — new `quickshell/modules/chat/GooseAcpSession.qml`. Every protocol detail was confirmed live via a hand-written Python JSON-RPC probe against the real binary *before* any QML was written (not guessed from the public ACP spec): bare newline-delimited JSON framing; `initialize` needs only `{protocolVersion:1}`; `session/new` needs `cwd` (+ empty `mcpServers` list) and returns a `sessionId` that's the same id format as `goose session list`; `session/set_mode` is genuinely live (no process respawn, confirmed via a real `current_mode_update` notification); `session/request_permission` arrives as a real agent-initiated request (UUID-string id, unlike our own integer request ids) and **genuinely gates tool execution** — held a real `echo` shell call pending, responded `allow_once`, watched real stdout + `tool_call_update{status:"completed"}` arrive only afterward. `agent_thought_chunk` is real but model-dependent — qwen2.5-coder:7b (non-reasoning) never emitted one in testing; `tool_call`/`tool_call_update` (including live streaming stdout via `toolNotification.live_output`) turned out to be the actually-useful "what is the agent doing right now" signal, more so than thought chunks. `Process.write()`/`stdinEnabled` (the QML API making this possible) were confirmed against the installed `quickshell-io.qmltypes`, not assumed.
- `ChatState.qml`/`ChatOverlay.qml` rewritten on top: real token-by-token streaming render, a client-side send-while-waiting queue (type and send a second message before the first finishes — it queues and auto-fires on `turnComplete`), lightweight tool-call visibility lines (`→ shell · ...` / `✓ completed`). Full thinking-panel UI, agent-mode toggle, permission-approval UI, and model selector are Stage 3+, not built yet — `permissionRequested` currently always auto-allows once, which is a safe no-op today since default `GOOSE_MODE` is `auto` (no permission requests fire in that mode at all).
- **Found and fixed two real bugs via live testing** (exactly the kind qmllint/build-clean never catches): (1) queuing a second message while busy displayed a *duplicate* user bubble on send, because the queued send re-ran the same "append user bubble" step that had already run when the message was first queued; (2) streaming text landed on whatever was the *last* array element, which broke as soon as a queued user bubble got appended after the in-flight assistant placeholder — the streamed reply silently glued itself onto the wrong bubble's text. Fixed by tracking `ChatState.streamingIndex` explicitly instead of assuming "last item," and by only appending the user bubble once regardless of queued/immediate path. Verified fixed by re-running the exact repro (two messages sent back to back) and confirming no duplicate bubble and correct text landing on each.
- **Found a second real config-sync bug while retesting with a faster model**: `goose-state-sync` (the existing dock/undock automation) only ever wrote the top-level `GOOSE_PROVIDER`/`GOOSE_MODEL` keys — but a `goose acp` session actually reads the *active model* from `providers.<provider>.model`, a separate key `goose-state-sync` never touched. This meant the chat overlay kept using the previous provider's model even after a "successful" dock/undock sync, silently. Fixed `goose-state-sync` to also write `providers.$provider.model` (guarded on that provider key existing in config.yaml). Re-ran the real dock reconciliation afterward (eGPU is physically plugged in again as of this session) — `qwen3.6:latest` is now correctly set in both places.
- All changes verified via `nh home switch` + full `nix flake check`, plus live round-trip testing in the real running Hyprland+Quickshell session (persistent single `goose acp` PID confirmed across multiple turns via `ps`, not just "build succeeded").

### Chat session-history picker (2026-09-12, Stage 2 of the chat-expansion plan)

- **Found the CLI's `goose session list` is the wrong data source**: every session this chat overlay creates gets `session_type: "acp"` in the sessions sqlite DB, and `goose session list`/`--format json` silently only returns `session_type: "user"` sessions — confirmed by comparing its output (6 rows) against a direct query of the raw DB (48 rows). Using it would have made every session the picker itself creates invisible in its own history list. Used the real ACP `session/list` method instead (confirmed live: needs no active session, returns richer per-session metadata than the CLI anyway — `sessionId`, `cwd`, `title`, `updatedAt`, `messageCount`, `providerId`, `modelId`).
- **`session/load`** (confirmed live, needs `sessionId`+`cwd`+`mcpServers`) replays a resumed session's entire prior history as a burst of `user_message_chunk`/`agent_message_chunk` notifications (each one a complete past turn, not a streaming delta — a real, distinct case from live-prompt streaming) before resolving with the same `modes`/`configOptions` shape `session/new` returns.
- New `quickshell/modules/sessions/{SessionsState.qml, SessionsPicker.qml, qmldir}` (`SUPER+H`) — two-pane list+preview popup, structural cousin of `Launcher.qml`/`ChatOverlay.qml` (centered `PanelWindow`, Overlay layer, click-outside/Escape). `GooseAcpSession.qml` gained `listSessions()`/`loadSession()` plus a `historyMessage`/`historyLoaded` signal pair to carry the replay burst distinctly from live `messageChunk`.
- **Live-verified end to end** (real mouse clicks via `wlrctl pointer move/click`, not just keyboard): opened the picker, saw real session titles/message-counts/models/timestamps render, selected one (preview pane populated correctly), clicked Resume (picker closed, chat reopened with the exact prior conversation replayed correctly), sent a new message, and confirmed the model's reply referenced real prior turn content — genuine context continuity, not a fresh disconnected session.
- **Found along the way**: a resumed session keeps *its own originally-recorded provider/model* (stored per-session in the sessions DB), not whatever's currently in `config.yaml` — resuming an old `qwen3.6:latest` session while the live default was `qwen2.5-coder:7b` correctly used the old, slower model, explaining an initially-alarming multi-minute wait that turned out to be correct behavior, not a bug.
- **Found and fixed a deployment mistake mid-session**: the first version of `SessionsPicker.qml` used `IpcHandler` without importing `Quickshell.Io`, which crashed Quickshell's *entire* config load (not just this module) — the whole desktop shell (bar, wallpaper, launcher, everything) went down until fixed. Worth remembering: a single new module's QML error can take down the whole shell, not just itself — restart-and-check-the-log immediately after any new module addition, don't assume isolation.

### Local coding-agent driver: root cause found and fixed, opencode evaluated (2026-09-13)

Picked up from a lost session that left the "genuinely reliable local primary driver" question open. This pass found and fixed two real, previously-undiagnosed root causes rather than just retesting the same setup — no eGPU attached throughout (38GB RAM, 16 CPU cores, all inference CPU-only).

- **Root cause #1, confirmed live: `ollama show qwen2.5-coder:7b` reports a trained context length of 32768, but nothing set `OLLAMA_CONTEXT_LENGTH`, so every model was silently capped at Ollama's own runtime default.** `journalctl -u ollama.service` showed `n_ctx_slot = 4096` while loaded — 1/8th the real capacity. Truncated tool schemas + history mid-task is a textbook explanation for the flaky symptoms logged below in the original driver comparison. A past session had already half-suspected this: `ollama list` had an orphaned `qwen3.6-32k:latest` (23GB) — `qwen3.6:latest` rebuilt via a custom Modelfile with `num_ctx 32768` baked in, created and never wired into anything. Fixed properly and globally instead: `modules/nixos/services/ollama.nix` now sets `services.ollama.environmentVariables.OLLAMA_CONTEXT_LENGTH = "16384"` — 16384 chosen over 32768 on RAM-safety grounds (32768 would push the largest pulled model's resident footprint, weights + KV cache, to ~33.5GB, at/over the ~33GB free when undocked with zero swap configured). Verified live across two different models (`qwen2.5-coder:7b`, `qwen3-coder:latest`) via `ollama ps`/`journalctl` both reporting `16384`, not `4096`. The orphaned `qwen3.6-32k:latest` was deleted (reclaimed 23GB) since it bakes in `num_ctx 32768` directly and would have bypassed the new safe global default entirely.
- **Root cause #2, found live during this session's own testing, not previously diagnosed at all: Goose's default extension set bloats the system+tool-schema prompt to ~15K tokens before the user's actual task is even counted.** With every bundled extension enabled (`playwright`, `orchestrator`, `summon`, `computercontroller`, `autovisualiser`, etc. — the live `~/.config/goose/config.yaml` state), a plain "read this file" request measured `task.n_tokens = 15477` in the Ollama server log — almost the entire (even the fixed) 16384-token context consumed by tool definitions the task never needed, leaving essentially no room for real reasoning. The same task with `goose run --no-profile --with-builtin developer` (loading only the file-read/write/shell extension actually needed for coding) cut the prompt to ~1.4K tokens and completed correctly in 25 seconds flat. This is likely a second, previously-unaccounted-for contributor to the original driver comparison's flakiness, independent of the context-length bug.
- **opencode evaluated and ruled out — confirmed dead via direct API comparison, not just repeated failures.** Installed via nixpkgs (`pkgs.opencode` 1.18.29, new `modules/home-manager/apps/opencode.nix`, declarative `opencode.json` pointed at Ollama's OpenAI-compatible endpoint) and config-verified (`opencode debug config`, `opencode models ollama` resolved all 5 candidate tags correctly). But every real `opencode run` invocation against a local model hung indefinitely with zero response, tool-call or not. Isolated the cause precisely: a raw `curl` to `http://localhost:11434/v1/chat/completions` with `stream:true` and a `tools` array completes cleanly with a proper `[DONE]` terminator, but the identical request shape sent through opencode's own `@ai-sdk/openai-compatible` client never resolves — a client-side bug in opencode itself, not a config or model problem. Matches unresolved upstream GitHub issues (#1068, #5694, #1034, #3029) reporting the same broken/no-response behavior against Ollama. Not something fixable from this repo; opencode stays installed (harmless, and its ACP mode may still be worth revisiting for a future cloud-model UI path) but is excluded from the local coding-driver decision entirely.
- **Test matrix result (Goose only, opencode excluded per above): `qwen2.5-coder:14b` is the practical winner.** 3 fixed tasks (fabrication check, multi-step edit+syntax-check chain, induced-error recovery) x 2 repeats, `--no-profile --with-builtin developer`, `OLLAMA_CONTEXT_LENGTH=16384`:
  - `qwen2.5-coder:14b`: 4/6 passed. **Zero fabricated tool calls across all 6 runs** — the original "prints tool-call JSON as literal text" symptom did not recur once. Both failures were cold-start latency hitting the 240s test ceiling (one on the very first inference call, one mid-task), not incorrect behavior — the same tasks passed cleanly in 85s and correctly-if-slowly once the model was already warm. Error-recovery (task C) passed both times.
  - `qwen3-coder:latest` (30B MoE): 2/6 passed, also zero fabrication, but a real new quirk: on one run it tried to open `toy.py` with a `read_image` tool ("unsupported image format"), caught its own mistake, but didn't complete the correction before the run ended. Less reliable than `qwen2.5-coder:14b` in this test, kept as a documented fallback rather than the primary.
  - Practical takeaway: with both root causes fixed, tool-calling *correctness* looks solved for `qwen2.5-coder:14b` — the remaining constraint is pure CPU-inference latency (cold model loads can exceed a few minutes), not agentic reliability. A real daily-driver setup should keep the model warm (Ollama's keep-alive) and expect first-call latency after any idle period.
- **Decision**: `qwen2.5-coder:14b` via `goose run --no-profile --with-builtin developer` (or the equivalent minimal-extension config wired permanently into `~/.config/goose/config.yaml`) is the recommended fully-local coding driver going forward. The 7B undocked default in `ai-workstation.nix` was never actually the bottleneck — the two root causes above were. Rewiring `ai-workstation.nix`'s undocked model tag and Goose's default extension set to match this finding permanently is a natural fast-follow, not done this pass (kept CLI-manual to conserve session budget — see below).

### Making Goose a real Claude-Code-style driver: recipe, wrapper, and a genuine CPU speed finding (2026-09-13)

Follow-up same day, once the driver was picked: the ask was to make Goose realistically *take over* coding/diagnostic work, not just pass toy tests. Two real, load-bearing pieces came out of this, plus one honest limit that isn't fixable from software.

- **`modules/home-manager/apps/goose.nix` gained a real Goose *recipe*** (`goose recipe validate` confirmed the schema live rather than guessing it — field names `version`/`title`/`description`/`instructions`/`prompt`/`parameters`/`extensions`/`settings` all confirmed against the installed 1.47.0 binary): a system prompt written specifically for a small model standing in for Claude Code — always use real tools instead of narrating them, read before writing, smallest correct change, match existing style, no line-by-line comments, verify every edit instead of declaring done on faith, investigate rather than guess when diagnosing, stop before anything destructive. Pinned to `qwen2.5-coder:14b`/`ollama` and the `developer`-only extension set from the finding above. Installed declaratively at `~/.config/goose/recipes/coding-agent.yaml`. `--recipe` can't be combined with `-t`/`--text` (confirmed by the CLI's own error) — task input goes through a `{{ task }}` template parameter and `--params task="..."` instead.
- **New `goose-code` wrapper** (same file): one-shot task via the recipe, then drops into `-s` interactive follow-up — confirmed live that `--recipe` and `-s` are compatible. This is the actual day-to-day entry point: `goose-code "do X"` from whatever directory you want it working in.
- **Real root cause of the biggest remaining gap, found live, not assumed: this laptop's CPU was running LLM inference in the `balanced` power-profiles-daemon profile, capping sustained clock speed to ~2.4GHz against a 4.7GHz max** (`cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_cur_freq` during a live generation, cross-checked against `scaling_max_freq`). Measured token-generation speed under this cap: **~3.4-4.0 tok/s** even for the 7B model — genuinely slow, and enough to make a multi-file diagnostic task blow past a 5-8 minute timeout more than once during testing. Switching to `powerprofilesctl set performance` (confirmed callable without `sudo`) gave a real but modest bump (~15%); CPU frequency under sustained multi-core AVX load still fell back toward ~2.4GHz shortly after, consistent with this being a thin 28W-class laptop chip (i7-1260P) hitting its own thermal/power envelope under sustained vector-heavy compute — not a governor misconfiguration, a real hardware ceiling. `goose-code` now switches to `performance` for the duration of a run and restores whatever profile was active before on exit (`trap ... EXIT`, verified live: profile flips to `performance` mid-run and back to the prior value afterward even when the run is killed early).
- **Real diagnostic test, not a toy task**: had `goose-code`'s recipe investigate the exact unresolved question flagged in §5 above (can `PamContext.start()` be called again directly after a `Failed` completion, or does `active` need toggling first) by reading the real installed `quickshell-service-pam.qmltypes` and `quickshell/modules/lock/LockService.qml` — no hints given beyond the file paths. First two attempts (`qwen2.5-coder:14b`, then a mis-pathed retry) timed out at 400-600s but showed genuine, correct tool use throughout (real `grep`/`cat` calls, no fabrication, and turned up a real fact neither of us had surfaced before: `PamError::Enum` is `["StartFailed", "TryAuthFailed", "InternalError"]` and `PamResult::Enum` is `["Success", "Failed", "Error", "MaxTries"]`). A clean run on `qwen2.5-coder:7b` (500s timeout, warm model) completed in full and gave a correctly-hedged answer — high confidence, correctly reasoned from what it actually read, explicitly noting the type signatures don't mention needing to reset `active` first. **This closes the §5 open question**: calling `pam.start()` directly after `Failed` is fine as written in `LockService.qml`, no toggle needed — confirmed by the same investigation this session ran to prove Goose could do it.
- **Honest limit, not spun as fixed**: raw CPU-only generation speed (~3.5-4 tok/s even after the performance-profile fix) means Goose will never *feel* as fast as Claude Code on this hardware while undocked — a task requiring 2+ tool calls plus a synthesized answer routinely takes 3-10 minutes. It is now genuinely *correct* (zero fabrication across every test this session, real tool use, appropriately-hedged answers) and *usable* for real work, just not fast. Treat it as "kick off a task, come back in a few minutes" rather than an interactive back-and-forth, until the eGPU is docked (untested this pass — no eGPU attached) or a faster local runtime path is found.

### Docked-state (eGPU) verification — the speed ceiling above is gone (2026-09-14)

eGPU connected same day. `ai-workstation-dock-sync.service` fired automatically on dock as designed (`/run/ai-workstation/state.json` correctly flipped to `docked`); confirmed real ROCm GPU inference live rather than assuming the dock alone was enough.

- **`qwen2.5-coder:14b` fully GPU-offloads**: `ollama ps` → `100% GPU`, 12.3GB/16GB VRAM used. Measured via `journalctl`: **~289 tok/s prompt processing, ~37 tok/s generation** — roughly 17x/10x the undocked CPU numbers above (16.67 tok/s prompt, 3.4-4 tok/s generation).
- **`qwen3-coder:latest` (30B MoE, code-specialized, 20GB) doesn't fully fit 16GB VRAM** — `ollama ps` shows a `78%/22% GPU/CPU` split — but still measured ~46 tok/s prompt / ~30 tok/s generation, nearly matching the fully-offloaded 14B, because MoE only activates a fraction of its total parameters per token. This makes the stronger, code-specialized model effectively free to use once docked, where undocked it wasn't (excluded from the CPU-only test matrix above precisely because its size made it impractical there).
- **`goose-code` (`modules/home-manager/apps/goose.nix`) is now dock-aware**: checks eGPU presence the same way `ai-workstation-gaming-stop` already does (`lspci -d 1002:73bf`) and picks `qwen3-coder:latest` when docked, `qwen2.5-coder:14b` otherwise — kept independent of `ai-workstation.nix`'s shared `GOOSE_MODEL` routing on purpose, since that value also drives the general-assistant chat overlay and the best coding model isn't necessarily the best quick-chat model.
- **Live-verified end to end**: a real 2-tool-call task (read a file, run `python3 -m py_compile` on it, report the result) in a real directory completed correctly in **8.9 seconds total** — down from the 3-10 minutes documented above for equivalent CPU-only tasks. The "kick it off and come back later" limitation from the undocked section is gone while docked.
- **One real hiccup, honestly not fully chased down**: an initial test where `qwen3-coder` used a `tree`-style directory-listing tool got a genuine "the model returned an empty response" from Ollama and the session ended — but that test also piped `/dev/null` as stdin into `goose-code`'s interactive (`-s`) mode to force a non-interactive run, which isn't a real usage pattern (a human at a real terminal would just see "please resend" and retype). A follow-up non-interactive (`--no-session`, no `-s`) run of an equivalent task completed correctly on the first try. Worth watching for during real interactive use, not confirmed as a systemic problem.
- **Found, not fixed: `powerprofilesctl set balanced` started failing mid-session** with `Failed to activate CPU driver 'intel_pstate': ... policy11/energy_performance_preference: Device or resource busy` — every retry hit the same error, specifically on `policy11` (one CPU core). This means `goose-code`'s exit trap can silently fail to restore the power profile (it's a `|| true`, so the script doesn't crash, but the system can be left in `performance` instead of whatever it was before). Left the machine in `performance` after this session — not harmful, just worse battery life than intended. Didn't chase the root cause (needs interactive `sudo` to restart `power-profiles-daemon` or investigate `policy11` specifically, neither doable non-interactively) — a plain reboot would very likely clear it. Worth a real fix later: either root-cause why one policy gets stuck, or have `goose-code` fall back to a direct sysfs write if `powerprofilesctl` fails.

### Full tooling + handoff attempt (2026-09-14)

- **`goose-code`'s recipe now has Claude-Code tool parity**: `developer` + `todo` + `summon` + `orchestrator` extensions (was `developer`-only), adding real task tracking and subagent delegation. Prompt overhead rose from ~1.4K to ~4.1K tokens — still well under the 16384 budget, correctness unaffected in testing.
- **Planner/coder model split, docked only**: `goose-code` now runs `qwen3.6:latest` as primary/orchestrator with `GOOSE_SUBAGENT_PROVIDER=ollama`/`GOOSE_SUBAGENT_MODEL=qwen3-coder:latest` for `Summon.delegate()` calls. Undocked stays single-model (`qwen2.5-coder:14b`) — CPU is too slow to justify juggling two models' load/evict cycles.
- **`~/.config/goose/config.yaml` providers trimmed to exactly two**: `ollama` and `claude-acp` (the old `local` and `claude-code` entries removed). Done partly by a local Goose agent (correctly handled the provider list), partly by hand (it left a dangling `GOOSE_SUBAGENT_PROVIDER: claude-code` pointing at a now-deleted provider — fixed to `ollama`/`qwen3-coder:latest`). `claude-acp` is verified wired correctly (reaches the real auth step and fails specifically with an OAuth-session-expired error) but needs a human to run `claude auth login` interactively before it actually works — no agent can do that (real browser OAuth flow).
- **Summon subagent delegation confirmed genuinely working**: a real `delegate` tool call, a distinct subagent process, and a real file write all happened end to end. **Real bug found**: the subagent used a full overwrite instead of appending, destroying the original file content, and the primary's own verification (`tail`) didn't catch the data loss. Delegated edits need real review, not blind trust.
- **`ai-workstation-boot-sync.service` added and confirmed live**: `systemctl is-enabled` → `enabled`, ran successfully at last boot. Reconciles dock/undock state even when no hotplug event fires.
- **Honest limitation found while trying to hand this off to the local model entirely**: asked `qwen3.6:latest` (via plain non-interactive `goose run --no-session`, after `-s` piped-`/dev/null` sessions were separately found to end early on any mid-task hiccup) to write this exact section and verify the boot-sync service itself. Twice, across two different tasks (this one, and the `ai-workstation.nix` edit above), it read the relevant files, sometimes built a `todo_write` plan, but the run ended without ever calling the edit/write tool — confirmed both times via `git diff` showing zero changes despite the process exiting cleanly (exit 0, no error, no timeout). Both edits above were completed directly instead rather than spend further turns on a third attempt. **This is the real remaining gap for "hand off entirely to local agents": non-interactive multi-step file-editing tasks don't reliably reach the execution step with the current model/harness combination** — real-time interactive use (a human watching, able to nudge it to continue) hasn't hit this same failure mode in any test this session. Worth investigating later: `--max-turns`, a different primary model for this specific task shape, or restructuring the recipe's instructions to be more forceful about not stopping after planning.

### Phase A/B/C: Nix-managed config, autonomous handoff validated, local-model reliability mapped (2026-09-16)

Full plan/findings log: `~/.claude/plans/read-through-section-7-crystalline-balloon.md` (a separate doc from the Phase 1a/1b one above). Three-phase structure: **Phase A** (Claude does the minimum to make Goose reliable), **Phase B** (Goose executes a supervised task ladder), **Phase C** (Goose drives the remaining work queue alone). 18 commits, `ai-workstation-phase-a` branch merged to `main`.

- **Phase A — the real fix, measured**: `config.yaml` rewritten as Nix-generated and static (`modules/home-manager/apps/goose.nix`'s `gooseConfig`), trimmed from 18 extensions to 2 (`developer`+`todo`; recipes still declare their own richer set, which *replaces* this at recipe-run time). Measured live via an isolated `GOOSE_PATH_ROOT` probe: **13,542 → 1,360 tokens (89.9% reduction)** for the same trivial task, time-to-first-token 30.4s → 3.7s. Also: a `retry:` block (`nix flake check` as the pass condition) actually wired into the coding recipe and confirmed firing automatically on both success and failure; `mcp-nixos` + `mcp-language-server`/`nixd` added to the coding recipe (real Nix/diagnostics tool access, not guessing); roster trimmed to 4 models (`devstral`, `llama3.2:3b` removed, ~18GB reclaimed); a `goose-claude` escalation wrapper and a `goose-plan` launcher (redesigned mid-build — plan mode turned out to be interactive-session-only, no `goose run` path exists at all, confirmed via binary symbols) added alongside the existing `goose-code`.
- **Phase A exit criterion passed for real**: `goose-code` (docked, `qwen3.6:latest`) autonomously fixed `ai-workstation.nix`'s stale `undockedModel` (`7b`→`14b`), self-verified via the retry gate's own `nix flake check` run, independently reconfirmed.
- **Phase B — 6/6 supervised tasks passed, zero recurrence of any previously-documented failure mode** (append-only, read-only report, edit+self-verify, `.agents/checks/*.md` authoring). The one real quirk: it occasionally guesses at a nonexistent `read` tool before self-correcting to `shell cat` — costs a wasted turn, not a correctness bug.
- **Real bugs found only by running things, never by `nix flake check`/`nixos-rebuild build` alone** (matching this file's own long history of the same lesson): `home.activation`'s `entryAfter ["writeBoundary"]` does **not** guarantee `home.file` symlinks exist yet — real order is `writeBoundary → (custom scripts) → linkGeneration` — broke the first live `nh home switch` with a literal "cannot stat" error; fixed by using a direct `pkgs.writeText` store path instead of the `home.file` target. A stdio recipe extension needs an explicit (even empty) `args` field or `goose recipe validate` rejects it. `goose run --params key=value` hard-fails (`Invalid recipe: did not find expected key`) if the value contains a literal `"` character — hit repeatedly this session, including in my own task prompts to `goose-code`, and is now documented in `.agents/checks/goose-cli-quirks.md`. `services.ollama.acceleration` no longer exists in this nixpkgs pin (hard assertion, not a warning) — `goose-code` caught this from a real build error after my own task instructions assumed it still existed. `goose local-models delete` reports success but doesn't touch `registry.json` at all — worse than previously documented ("unregisters but doesn't delete"); fixed by editing the registry directly.
- **Corrected two of my own earlier design assumptions**: `claude-acp` (the provider originally wired for Claude escalation) needs a separate npm-installed ACP adapter; `claude-code` is a native, adapter-free Goose provider needing only the `claude` CLI itself authenticated — switched to it. `services.ollama.acceleration` — see above.
- **Ollama tuned and safety-tested before enabling**: `OLLAMA_FLASH_ATTENTION=1`+`OLLAMA_KV_CACHE_TYPE=q8_0` verified via a standalone throwaway `ollama serve` instance (never touched the live service) — clean startup, correct coherent inference, zero errors on this gfx1030 GPU. `OLLAMA_KEEP_ALIVE=30m`, `OLLAMA_MAX_LOADED_MODELS=2` also set.
- **`goose-bench` (lean v1) added** — a single-cell benchmark primitive (`goose-bench MODEL [PROVIDER] [LABEL]`), not the full matrix from the original plan. Notable: `goose-code` (qwen3.6:latest, docked) could not write this ~90-line new file at all — spent its whole turn exploring, including a non-sequitur `mcp-nixos` search for a "goose bench" package, and produced zero output before hitting its token limit. Every other Phase C item needed *editing* an existing file and mostly succeeded (sometimes with a fix-up); this is the first that needed *authoring new code from a spec* and didn't produce anything — a real, reproducible capability boundary, not a one-off. Written directly instead. A live test of the resulting harness then caught a real bug in it: `grep -c` always prints a count but exits 1 at zero matches, and an `|| echo 0` fallback double-printed it, corrupting the JSON output — fixed, then reverified with a genuine result, a second run, and a resumability check.

### Undocked model-reliability investigation (2026-09-16) — root cause found, no clean fix yet

Prompted by wanting to confirm the local agents are "genuinely useful, fast enough" for a Claude Code replacement — tested against a real multi-file Rust/web/MCP task (`~/Projects/goose-quality-eval`, later abandoned in favor of this narrower diagnostic once the real signal showed up on a *trivial* control task instead).

- **`qwen2.5-coder:7b` and `:14b` — the only CPU-viable undocked models — reliably fail to invoke tools at all on anything past pure Q&A.** Both print well-formed tool-call JSON as inert prose text instead of calling the tool, reproducibly, across every recipe/extension-count/task-size combination tested (including the exact proven-good `coding-agent.yaml` recipe on a task as small as "append one line"). Ruled out as the cause, in order: task complexity, my custom recipe, extension/tool-schema size, system/thermal/GPU health. **The actual differentiator, confirmed by swapping only the model**: the identical failing task, same recipe, same everything, run against `qwen3.6:latest` instead — succeeded cleanly with real tool calls. This is a genuine Qwen-generation capability gap in native tool-calling, not a config or prompt problem.
- **The toolshim (Goose's built-in rescue mechanism for exactly this symptom) doesn't cleanly fix it.** Its hardcoded target model (`mistral-nemo`) had never actually been pulled — pulling it and enabling `GOOSE_TOOLSHIM=true` did get `qwen2.5-coder:14b` to start emitting real tool calls, but it then misread the task itself and hallucinated nonexistent tool names (`cat`, `sed`, `read_line`) alongside real ones, at 2-4 tok/s instead of ~30+. Converts a silent failure into a slower, noisier, still-usually-unsuccessful one.
- **A same-generation smaller model (`qwen3:8b`, 5.2GB) does call tools reliably**, including under true CPU-only inference (verified via an isolated `HIP_VISIBLE_DEVICES=""` throwaway Ollama instance — not just assumed from being "small"). But: (a) Qwen3's thinking-mode overhead plus genuine CPU slowness made a single trivial one-line append take up to 8 minutes end to end, and (b) on the first attempt it picked `write` (full overwrite) instead of an append operation and destroyed the test file's existing content — the exact overwrite-vs-append data-loss bug this whole project has been guarding against. A real `Ollama` API/Goose env-var lever exists (`think: false` API param; Goose's own `GOOSE_LOCAL_ENABLE_THINKING`/`GOOSE_THINKING_EFFORT`, found via `strings` on the binary) and measurably helped on GPU when combined with an explicit anti-overwrite instruction, but a clean CPU-only confirmation was inconclusive (a leftover process from incomplete test cleanup, not a re-tested failure) and wasn't pursued further given the cost (minutes per iteration).
- **Practical conclusion, not fixed by tuning**: undocked stays a batch medium for `qwen2.5-coder`, and read-only-safe for anything else — don't trust it for autonomous multi-step edits. `qwen3:8b` with thinking disabled is the closest thing to a working undocked coding option today, but budget minutes-per-edit, not seconds. Docked remains the only setup where real coding work has actually succeeded reliably (everything in Phase A/B/C above).

### Chat overlay live model-switching wired (2026-09-16)

Direct follow-up: found that neutering `goose-state-sync`'s `config.yaml` writes earlier the same session (correctly, per Phase A above) had also silently killed the only mechanism that kept the chat overlay's *actual served model* in sync with dock state. Confirmed live: the running `goose acp` backend process had been alive since boot, still serving the pre-session 18-extension config and boot-time model, completely unaffected by any config or dock-state change since.

- `GooseAcpSession.qml` already had a working `switchModel(provider, model)` function (env-based process restart + `session/load` resume) from an earlier session — nothing called it automatically. Added a `goose-model` `IpcHandler` target exposing it (same convention as `ChatOverlay.qml`'s existing `chat`/`toggle`), and wired `goose-state-sync` to call it via `quickshell ipc call goose-model switchModel <provider> <model>` on every dock/undock.
- **Verified fully live** in the actual running Hyprland+Quickshell session, not just build-clean: the IPC target registered correctly (`quickshell ipc show`), a real call spawned a fresh `goose acp` process with the correct env vars (confirmed via `/proc/<pid>/environ`) that correctly picked up the trimmed config, and a real message round-tripped end to end (`wtype` simulating input → real Ollama request in the journal → real response) with the `sessions`/`lock` IPC targets confirmed still responsive throughout — zero disruption to the live shell from a QML edit while it was running.
- **Surfaced but not yet fixed**: the same Qwen3 thinking-mode overhead from the undocked investigation above hits the chat overlay too — "what is 9 times 8" took 1704 tokens and 1m18s wall-clock through `qwen3.6:latest` despite ~32 tok/s raw generation. `GOOSE_LOCAL_ENABLE_THINKING=false` hasn't been wired into the overlay's model-switch path yet.
- **Three separate `goose-code` attempts at the `GooseAcpSession.qml` edit itself all failed** before it was done directly — once by writing a full architectural summary instead of editing, once by tripping the `--params` double-quote bug in my own task text, once by correctly locating the exact insertion point via real tool calls but running out of turns before ever calling `edit`. Consistent with the `goose-bench` finding above: new-code and precise-insertion tasks are markedly less reliable than straightforward file edits, even docked.
- Lock screen (`LockService.qml`, `LockView.qml`, `quickshell-lock.nix` — new this session, never previously live-verified) and the session-history picker were checked for structural soundness (both IPC targets present and responsive; Quickshell never crashed across the whole investigation, which it would have on a real QML error per this file's own earlier lesson) but a live lock trigger was deliberately not attempted — no remote way to unlock it back if something went wrong.

### Still open
- Set up OpenRouter/Anthropic credentials — `goose configure` requires a real interactive terminal (confirmed: it hard-errors outside a TTY), so this can only be done by a human, never delegated to Goose itself.
- **Reboot to clear the live stuck `llama-server` D-state process documented above** — genuinely blocking (can't be done autonomously without the user present). Not re-checked this session.
- Verify Goose Desktop actually picks up a `config.yaml` change on next launch/new-session (the wrapped launcher's synchronous sync call is unverified against a real Desktop window) — Desktop was confirmed to correctly pick up the persistent `playwright` extension on its own launch, at least.
- End-to-end test `SUPER+G` through a real Steam/gamescope launch — `ollama stop` VRAM eviction and post-game state reconciliation are only unit-tested so far.
- Finish the deferred Phase 1a checklist (D3 compute control, undocked clean-boot check, D3 surprise-unplug, suspend/resume) — genuinely needs physical eGPU docking/undocking and a real suspend/resume cycle, none of which are autonomously doable.
- Wire `GOOSE_LOCAL_ENABLE_THINKING=false` (or a `GOOSE_THINKING_EFFORT` tier) into both `goose-code` and the chat overlay's `switchModel` path — real, measured lever for the Qwen3 thinking-mode latency tax found twice this session (undocked `qwen3:8b`, docked `qwen3.6:latest` in the chat overlay), not yet wired anywhere.
- Root-cause why non-interactive local-agent runs sometimes stop after planning instead of executing — still open. This session found a *related*, more specific failure (tool-call-as-prose on `qwen2.5-coder`) but did not confirm it's the same root cause as the original "stops after planning with no error" symptom on `qwen3.6:latest` documented above; genuinely untested this pass.
- Live-trigger the lock screen end to end (with the user physically present to actually unlock it) — structurally checked but never actually invoked this session, deliberately.
- Consider whether `qwen3:8b` (or another Qwen3-family small model) should replace `qwen2.5-coder:7b`/`14b` in the undocked roster now that a real, reproducible tool-calling reliability gap is confirmed between the two model generations — not decided, needs the thinking-mode/append-vs-overwrite issues resolved first.

---

## 8. Bar widgets — click-driven flyouts

Same upgrade `Volume.qml` already got over launching `pavucontrol` directly, applied to the other two bar icons that previously just shelled out to an external app on click.

- **Network** (`NetworkPopup.qml`): lists `Quickshell.Networking` Wi-Fi networks sorted by signal strength, real API confirmed via the installed qmltypes (`Network.{connect,disconnect,forget}`, `WifiNetwork.{signalStrength,security,connectWithPsk}`, `WifiDevice.scannerEnabled` toggled on while the popup's open so the list reflects a live scan, not a stale cache). Known networks and open/OWE ones connect with one click; an unknown secured network expands an inline password field in place (`connectWithPsk`) instead of a separate dialog. `nmtui` stays one click away via a "›" link for anything this doesn't cover (hidden SSIDs, enterprise auth).
- **Bluetooth** (`BluetoothPopup.qml`): lists already-known/paired devices (`adapter.devices`) with click-to-connect/disconnect and battery percentage where available. Pairing a genuinely new device isn't covered — `BluetoothAdapter` has no direct discovery-trigger method in this API (only a read-only `discovering` status), so a real scan/pair UI wasn't buildable this pass without something to drive it; `blueman-manager` is the escape hatch for that case instead of a half-built pairing flow.
- Both follow `VolumePopup.qml`'s exact structural pattern (`PopupWindow`, `HoverHandler`-driven auto-close, `BarIcon.onClickFn` toggle) — no new popup architecture introduced.
- Validated via `qmllint` (no hard errors, only the same import-path noise the whole codebase's Quickshell-typed files produce under a bare invocation — confirmed by running it against the already-shipped `Launcher.qml` for comparison) and a full `nix flake check`/`nixos-rebuild build`. Not yet clicked on a real screen.
